DirectFB-media-samples is built using Meson.

First, configure the build:

  $ meson setup build/

Start the build with:

  $ meson compile -C build/

And install DirectFB-media-samples using:

  $ meson install -C build/
